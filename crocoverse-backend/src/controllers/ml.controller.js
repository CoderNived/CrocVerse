import { sendSuccess } from '../utils/apiResponse.js'

export const weightPredictor = (req, res) =>
  sendSuccess(res, null, 'Route working — implementation coming')

export const ageClassifier = (req, res) =>
  sendSuccess(res, null, 'Route working — implementation coming')

export const conservationPredictor = (req, res) =>
  sendSuccess(res, null, 'Route working — implementation coming')

export const habitatSuitability = (req, res) =>
  sendSuccess(res, null, 'Route working — implementation coming')

export const observationTrend = (req, res) =>
  sendSuccess(res, null, 'Route working — implementation coming')

export const populationRisk = (req, res) =>
  sendSuccess(res, null, 'Route working — implementation coming')

export const speciesIdentifier = (req, res) =>
  sendSuccess(res, null, 'Route working — implementation coming')