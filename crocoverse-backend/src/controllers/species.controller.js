import { sendSuccess } from '../utils/apiResponse.js'

export const getAllSpecies = (req, res) =>
  sendSuccess(res, null, 'Route working — implementation coming')

export const getSpeciesById = (req, res) =>
  sendSuccess(res, null, 'Route working — implementation coming')

export const createSpecies = (req, res) =>
  sendSuccess(res, null, 'Route working — implementation coming')

export const updateSpecies = (req, res) =>
  sendSuccess(res, null, 'Route working — implementation coming')

export const deleteSpecies = (req, res) =>
  sendSuccess(res, null, 'Route working — implementation coming')